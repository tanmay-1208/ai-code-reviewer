# PostgreSQL connection string
DATABASE_URL="postgresql://user:password@localhost:5432/ai_code_reviewer?schema=public"

# Groq API key (get one free, no credit card, at https://console.groq.com)
GROQ_API_KEY="gsk_..."

# Optional: GitHub personal access token, raises rate limits & allows private repos
GITHUB_TOKEN=""
